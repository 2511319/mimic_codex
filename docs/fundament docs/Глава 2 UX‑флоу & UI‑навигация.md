# 2\. UX‑флоу & UI‑навигация



## 2.1 Точки входа и «Продолжить»

**Вход через чат:** Основной способ запуска — из личного чата с ботом Telegram. Пользователь нажимает команду /start или спец-кнопку бота, после чего бот отправляет inline-кнопку **«Open RPG-Bot»** для открытия Mini App. При первом запуске показывается экран согласия 18+ (без принятия согласия доступ запрещён). После этого происходит перенаправление в веб-интерфейс приложения.

**Deep-links:** Поддерживаются глубокие ссылки с параметрами запуска. Например, start-параметр с adventure_id может сразу открыть конкретное приключение (библиотека модулей) или начать игру по сценарию. Параметр с invite_code направляет пользователя прямиком в групповой режим (пати) для присоединения к кампании по коду приглашения, минуя главное меню. Аналогично, если разработчик или админ рассылает ссылку на товар/магазин, Mini App откроется сразу на экране покупки.

**Состояние «Продолжить кампанию»:** Если у пользователя уже есть активная незавершённая игра, на старте приложения он увидит кнопку **«Продолжить кампанию»**. Нажав её, игрок мгновенно возвращается к последней сцене своего приключения (либо к бою, если прервался посреди боя). При продолжении загружается текст последней сцены, последние броски и актуальное состояние персонажа, чтобы освежить контекст. Если же активной сессии нет, пользователь попадает в обычное главное меню.

**Сохранение и восстановление сессии:** Идентификатор текущей кампании (и персонажа) хранится на стороне сервера и дублируется локально (например, в localStorage) для быстрого доступа. Локальный кеш сессии имеет TTL (временной-to-live), например 7 дней, после которого приложение может не предлагать автопродолжение без запроса к серверу. На сервере прогресс кампании хранится постоянно (Postgres). Правила восстановления такие: если сессия активна (не завершена пользователем) и не истёк лимит неактивности, при новом запуске выводится опция «Продолжить»; иначе пользователь начинает новую игру. Кешируются ключевые данные: campaign_id, этап (сцена/бой), последние результаты бросков. При повторном входе без валидного локального ключа приложение может запросить у сервера статус последней кампании пользователя и предложить восстановление вручную.

**UX-диаграмма входа:**  

Пользователь (TG чат) ── /start/кнопка ─→ Бот RPG-Bot ─→ \[Открыть WebApp\]  
↓  
(Первый вход?) — Да → Экраны онбординга (18+ согласие)  
↓ Нет  
(Параметр запуска?) — Да → Глубокая ссылка (см. ниже)  
↓ Нет  
\[Проверка активной кампании\]  
↓  
Да: показать «Продолжить» → Загрузка сессии и переход к сцене  
↓  
Нет: перейти в Главное меню (стартовый экран)

_Примечание:_ Если Mini App запущен с deep-link параметром, ветка **«Продолжить»** игнорируется — пользователь сразу попадает в указанный режим (например, присоединение к пати). Экран согласия 18+ показывается в любом случае единожды перед любым геймплеем.

## 2.2 Карта путей: Соло / Пати / Мастер / Редактор / Настройки

### Роли и пользовательские пути

Приложение охватывает несколько типов пользователей (ролей), для каждого предусмотрен свой UX-поток:

- **Соло-игрок:** хочет быстро начать одиночное приключение без лишних настроек.
- **Игрок пати:** присоединяется к кампании с другими игроками, принимает групповые решения.
- **Живой Мастер (Co-DM):** ведёт игру как ведущий, редактируя сцены и контролируя исходы в режиме со-ведущего.
- **Редактор/Автор:** создаёт и настраивает контент (кампании, сценарии, персонажи, NPC, лут, награды) вне активной игры.
- **Админ проекта:** имеет административные функции - отдельная админ панель web (настройка провайдеров, тарифов, модерация), однако напрямую в UX приложения роль админа не отражена для обычных пользователей.

Далее описана единая навигационная карта приложения, отображающая основные экраны и переходы между ними для всех ролей.

### Навигационная схема и экраны

Основные разделы приложения соответствуют пунктам главного меню. Ниже перечислены экраны и переходы между ними, включая состояния загрузки, ошибки и ключевые CTA (call-to-action):

- **Главное меню (стартовый экран):** начальный раздел, откуда пользователь выбирает дальнейший путь. Содержит кнопки/вкладки:
- **Соло** («сыграть за 2 минуты») – начало одиночной игры.
- **Играть в пати** – групповой режим (подменю: «Создать кампанию» или «Ввести код»).
- **Я — Мастер** – режим ведущего (создать новую кампанию с ролью Co-DM или подключиться к существующей в роли мастера).
- **Редактор** – режим создания контента (переход к редактору кампаний/NPC и т.д.).
- **⚙️ Настройки** – экраны настроек приложения.  
    _Состояния:_ Главное меню отображается после входа (если нет автопродолжения). В случае отсутствия интернета или сбоя API – показывается сообщение об ошибке загрузки меню и CTA «Обновить» для повторной попытки. В пустом состоянии (напр., нет данных) – могут показываться приветственное сообщение или инструкция.
- **Соло – Создание персонажа:** если выбран соло-режим, запускается мастер создания героя. Пользователю предлагается два варианта:
- **A) Полная анкета** – подробное создание персонажа (выбор расы, класса, распределение характеристик и т.п.).
- **B) Быстрый лист** – генерация персонажа по минимальным вводным (для быстрого старта).  
    _CTA:_ переключатели между режимами A/B, кнопка подтверждения создания.  
    _Переходы:_ После создания персонажа – автоматический переход на экран **Сцена** (старт игры).
- **Экран Сцена (Narrative Run):** основной экран игрового процесса вне боя. Здесь отображается текст текущей сцены (описание ситуации) и варианты действий. Ключевые элементы UI:
- **Текст сцены:** с форматированием (может включать **жирный** шрифт, _курсив_, встроенные ссылки на лор). Объём ~1000–3000 символов. После вывода текста мгновенно запускается фоновые задачи по медиа (см. ниже) – они не блокируют ввод.
- **Варианты действий:** всегда ровно **3 кнопок** с пронумерованными вариантами {1..3}, отражающими возможные решения героя. Одно из действий нарочно «блестящее/нелепое/опасное» для разнообразия повествования. Помимо 3 кнопок, есть кнопка **«✍️ Свой ход»**, позволяющая игроку ввести кастомное действие текстом (или фразу персонажа в кавычках). При вводе пользовательского хода система парсит его (распознаёт {...} как действие или “...” как речь персонажа).
- **Броски:** при выборе действия игра автоматически совершает необходимые проверочные броски кубов (d20 и другие) с учётом модификаторов. Результат показывается сразу внизу сцены в блоке **«Броски»**. Этот блок содержит краткое описание: например, _«Стелс: d20+3 = 16 (13+3)»_, а также иконки успеха/неуспеха. Он кликабелен для деталей (показывает, например, наличие **преимущества/помехи**, критические успехи/провалы и пр.). Все броски прозрачны для игрока.
- **Медиа-контент:** по умолчанию сцена включает только текст, но доступны опциональные медиа. Через ~1–3 секунды после текста могут подгружаться:
    - **🎧 Озвучка (TTS):** кнопка проигрывания аудио появляется рядом с текстом сцены. Нажатие воспроизводит озвучивание сцены голосом (язык и голос настраиваются). Автовоспроизведение **не происходит**, кроме особых случаев (см. Настройки). Короткие реплики NPC или системные сообщения **могут** воспроизводиться автоматически, если включено пользователем.
    - **Интерактивные диалоги с NPC** рассмотреть возможность создания блока интерактивного голосового чата, где ИИ отыгрывает роль NPC и итог разговора влияет на сюжет.
    - **Изображение сцены/портрет NPC:** сцены и NPC в основном предсозданны ви подгружаются из библиотеки приключений, но может и быть рассмотрен вариант, когда вводится новый значимый персонаж или локация, система заранее за 1-2 сцены до "прихода" игроков готовит интерактивные материалы, либо заготовки для них, а после **асинхронно** генерирует иллюстрацию (портрет или фон-сцену). то есть первым появляется текст который погружает игроков а атмосферу, после появляется изображение сцены и прочий интерактив. По готовности изображения они отображаются встроенно (под текстом или сбоку) с небольшим задержкой. Если авто-арт недоступен (например, превысили лимит) – отображается кнопка **«🖼️ Сгенерировать арт»**. При нажатии запрашивается подтверждение списания ресурсов (игровые **Stars**/_юниты_) за генерацию картинки. После подтверждения UI показывает индикатор загрузки, затем подставляет сгенерированное изображение. Все сгенерированные медиа также сохраняются в журнал сцены (для повторного доступа без повторных списаний).
- **AI-ярлыки (чипы):** на панели действий могут отображаться интерактивные ярлыки-подсказки от ИИ. Например, кнопки **\[ASK\] Спросить совет**, **\[TACT\] Тактическая подсказка**, **\[RULE\] Правило**, **\[LORE\] Лор**, **\[GEN-T\] Сгенерировать текст** и т.д. Эти ярлыки позволяют быстро получать дополнительную информацию или сгенерировать побочный контент:
    - _Guidance_ (совет): \[ASK\] – общий совет от ведущего; \[TACT\] – подсказка по тактике в бою или сцене.
    - _Knowledge_ (знание): \[RULE\] – пояснение правила из SRD; \[LORE\] – справка о лоре/мире; \[EXPL\] – объяснить, \[SUMM\] – краткое резюме событий.
    - _Creation_ (генерация): \[GEN-T\] – дополнительное описание или текст по запросу; \[GEN-I\] – арт-картинка; \[GEN-V\] – сгенерировать голосовое сообщение; \[GEN-MAP\] – набросок карты; \[GEN-NPC\] – создать нового NPC; \[REWRITE\] – переформатировать текст.
    - Эти чипы отображаются контекстно. В **режиме сцены** обычно видны \[ASK\]\[TACT\]\[RULE\]\[LORE\]\[GEN-T\]\[GEN-I\]\[GEN-V\] и кнопка «AI…» для открытия расширенной панели инструментов ИИ. В **режиме боя** набор упрощён до \[TACT\]\[RULE\]\[SUMM\] (тактика, правило, суммирование ситуации) и отдельная кнопка карты \[GEN-MAP\] для визуализации. В экране персонажа/инвентаря могут появляться \[COACH\] (совет по развитию) или \[RULE\] на карточках предметов/навыков. Для библиотеки приключений/новой кампании предусмотрен ярлык \[RECOM\] (рекомендации по выбору).
- **Переход хода:** когда игрок нажимает на один из вариантов действий или вводит свой, интерфейс переходит в состояние «ожидание ответа». Кнопки отключаются (может показываться индикатор прогресса или сообщение типа «RPG-Bot думает...») пока генерируется исход сцены. Если ответ получен успешно – отображается новая сцена (переход по циклу), если произошла ошибка (например, таймаут LLM) – игроку показывается уведомление об ошибке и предлагается повторить действие (CTA «Повторить попытку») или откатиться к предыдущему шагу. В случае **офлайн**\-режима (потеря связи) – приложение уведомляет о невозможности продолжить и предлагает повторно соединиться, либо сохраняет ввод до восстановления связи.
- **Fail-forward:** дизайн игры предполагает, что история движется вперёд независимо от успеха или провала действий. С точки зрения UI, после каждого действия всегда наступает новая сцена или эпизод (а не «Game Over»), пока персонаж жив. Даже при провале проверок интерфейс показывает последствия и открывает следующий выбор, поддерживая непрерывный поток игры.
- **Экран Бой (Combat mode):** отдельный экран, запускающийся когда в истории происходит сражение. Переход со сцены на бой происходит автоматически по триггеру (например, сцена инициирует бой) – UI переключается на режим боя. Основные элементы:
- **Трек инициативы:** вверху отображается порядок ходов (инициатива) для всех участников боя – персонажа(ей) и NPC. Ход текущего участника подсвечен.
- **Панель действий боя:** в центре или снизу экрана – кнопки действий персонажа в бою: атаковать, применить навык, использовать предмет, переместиться, защититься и т.д. (соответствует упрощённой боевой системе Combat-Lite). Если у персонажа есть способности или заклинания, они также доступны. **Реакции** могут отображаться отдельной кнопкой или выпадать при условиях.
- **Статус и лист персонажа:** сбоку может выводиться краткая сводка по персонажу – здоровье (HP), эффекты (статусы, например Оглушение, Кровотечение и пр.), основные характеристики. Отсюда же можно открыть полный **лист персонажа** или **инвентарь** (в модальном окне) для подробностей.
- **Спец. элементы для Co-DM:** если в бою присутствует живой мастер, у него могут быть дополнительные контроли: форсировать очередь или добавить NPC на лету.
- **AI-ярлыки в бою:** как упоминалось, доступны \[TACT\], \[RULE\], \[SUMM\], а боковая кнопка карты \[GEN-MAP\] позволяет запросить генерацию тактической схемы/карты боя (например, схематичный план локации с позициями).
- **Завершение боя:** при выполнении условий окончания (враги побеждены, либо игроки сбежали, либо достигнута иная цель) бой завершается. UI показывает финальный результат боя (например, «Победа!» или описание исхода) и выполняет **переход обратно на экран сцены** для продолжения повествования. Этот переход fail-forward: даже если бой проигран, история продолжится новой сценой (например, пленение персонажей) вместо простой смерти, если это предусмотрено сценарно. Однако смерть персонажа приводит к концу кампании или специальной сцене эпилога.
- **Экран Лобби пати:** если выбран режим партии (**«Играть в пати»**), сначала пользователь либо **создаёт новую кампанию**, либо **вводит код** для присоединения.
- **Создание кампании (пати):** пользователь, создающий кампанию, становится хостом. Ему предоставляется код приглашения, который нужно разослать остальным игрокам (вне приложения, например, в чате). После создания он попадает в **лобби** – экран ожидания, где видны слоты для игроков. Можно задать имя кампании или другие настройки (опционально). В лобби хост видит свой слот (персонажа) и свободные слоты для других. **Пустые состояния:** Если других игроков ещё нет, отображается подсказка “Ожидание игроков… поделитесь кодом: XXXX”. Как только игроки подключаются (введя код с главного меню), они появляются в списке лобби. У каждого участника показывается имя персонажа или пометка, что нужно создать персонажа.
- **Присоединение по коду:** пользователь вводит действующий invite-code и переходит в соответствующее лобби. Если кампания рассчитана на конкретное число игроков и все места заняты, отображается ошибка “Кампания полна” или если код неверный – “Код не найден”. При успешном входе игрок выбирает/создаёт своего персонажа (аналогично соло, но возможно с огран. по уровню или предгенами от мастера) и занимает слот.
- **Начало игры в пати:** когда все готово (хост решил начать), кампания запускается – все участники переключаются на экран **Сцена** синхронно. В пати-режиме повествование общое, но выбор действий происходит посредством **голосования**: каждый игрок видит те же 5 вариантов действий и кнопку «Свой ход». Они голосуют за один из вариантов; на это отводится таймер (напр. 30 секунд, значения варьируются). По умолчанию побеждает опция, набравшая простое большинство голосов【6†L105-113】. Если время вышло и голосов мало, возможны правила по умолчанию (например, если только один голос – его и принять). **Ничья** решается броском d20 между равными вариантами либо решением мастера.
    - **Свой ход в пати:** если игрок ввёл кастомное действие/реплику, оно также учавствует в голосовании – другие могут поддержать его, выбрав специальную опцию **«✅»** рядом с этим предложением (либо каждый может ввести свой вариант, но чтобы не затягивать, может быть ограничение в один кастомный ход на сцену).
    - **Роль Co-DM:** если кампания запущена с живым мастером (хост нажал «Я — Мастер» при создании), то мастер имеет право **оверрайдить** результат голосования или броска. В интерфейсе мастера при голосовании показывается кнопка «Принудительно выбрать \[номер варианта\]» на случай, если группа в тупике или мастер подготовил другой исход.
- **Оффлайн и повтор соединения:** так как пати-режим подразумевает синхронность, потеря связи у одного из игроков отображается остальным (напр. значок «игрок off» возле имени). Его персонаж может действовать автоматически (скипается ход или голос без него) если в настройках хоста включён авто-режим. При возврате в онлайн игрок может снова вступить в текущую сцену (приложение должно подхватить текущее состояние через сервер). В случае сбоя у мастера – игра ставится на **паузу** до его возвращения либо до передачи прав другому.
- **Экран Мастера (Co-DM панель):** для живого ведущего интерфейс игры дополняется специальными элементами:
- **Черновик сцены:** перед тем как сцена покажется игрокам, мастер видит её предварительно (черновой вариант). В этот момент у него есть инструментарий для правок:
    - Редактирование текста сцены непосредственно в окне (например, поправить стиль или содержание).
    - Подсветка результата броска и возможность **Override** – изменить исход проверки или броска вручную. Например, мастер может повысить результат броска (это логируется в системе как вмешательство).
    - **Вставить NPC/событие:** кнопка, позволяющая добавить в сцену внезапное событие или диалог от нового NPC. При нажатии мастер выбирает из библиотечки NPC (или генерирует нового) и вставляет его реплику или описание в текущую сцену.
    - Отметить элемент сцены как **«скрытый мотив»** – т.е. пометить определённый абзац или факт как известный только мастеру/ИИ, чтобы игрокам это не было явно видно (например, настоящий замысел NPC).
- **Публикация сцены:** после внесения правок мастер нажимает **«Опубликовать игрокам»**, и сцена становится видна всем игрокам пати. Далее идёт обычный цикл выбора действий/голосования. Если мастер не вмешивается, сцены могут публиковаться автоматически сразу (т.е. Co-DM может вести пассивно, давая ИИ работать).
- **Retcon (откат хода):** мастер имеет возможность откатить игру на один шаг назад, если, например, вышла ошибка или нужно изменить принятый ранее выбор. В UI это может быть кнопка «⟲ Отменить последнюю сцену». При нажатии у всех игроков откатывается состояние на предыдущую сцену (предыдущий узел истории), и мастер может запустить новую ветку. Данная функция ограничена и должна логироваться (чтобы не злоупотребляли). Retcon возможен только до начала следующего шага игроков.
- **Пауза игры:** мастер/хост пати может поставить игру на паузу (например, если кто-то отошёл). В паузе таймер останавливается, и интерфейс игроков отображает индикатор паузы. Снятие паузы возобновляет таймер и игровой процесс.
- **Прочие панели:** в режиме мастера доступна вкладка «AI» с генеративными инструментами: мастер может вызывать те же \[GEN-\*\] функции (генерация текста, NPC, карт) через отдельную панель, а также видеть подсказки от модуля Director (напр. советы по балансу сцены, авто-подбор сложности проверок). Эти функции встроены в интерфейс Co-DM меню.
- **Экран Редактор (библиотека и инструменты автора):** этот раздел предназначен для создания и управления контентом вне активной игры.
- **Список кампаний/сценариев:** отображается каталог имеющихся у пользователя кампаний. Пустое состояние: “У вас пока нет кампаний – создайте новую!” + CTA **«Новая кампания»**. Каждая кампания в списке показывает название, описание, метки (жанр, длительность, режимы). Для каждой есть опции: редактировать, поделиться, удалить.
- **Конструкторы контента:** внутри редактора доступны подразделы для создания:
    - **NPC:** форма с полями имя, описание, секреты (2 поля: лёгкий и трудный секрет), подсказка голоса, а также загрузка или генерация портрета NPC.
    - **Локации:** описание места, ключевые особенности, список обязательных событий и случайных событий; можно прикрепить фон-арт локации (или сгенерировать).
    - **Шаблоны сцен:** заготовки сцен с особыми элементами (например, типовые ловушки, фракции, сюжетные хуки), которые могут вставляться в кампанию.
    - **Персонажи (преген)**: предсозданные персонажи, доступные игрокам (с листами, историей).
    - **Импорт/экспорт:** возможность импортировать кампанию из файла JSON или экспортировать текущую (для обмена вне системы).
- **Генерация медиа в редакторе:** при создании контента автор может использовать генерацию артов – например, кнопка **\[GEN-I\]** рядом с полем портрета NPC или фона локации. При этом тоже действует подтверждение списания юнитов и учёт лимитов.
- **Ошибки/оффлайн:** при сбое сохранения показывается toast-уведомление “Ошибка сохранения, попробуйте еще раз”. Если сети нет – редактирование возможно в ограниченном режиме (offline-копия), с последующей синхронизацией при соединении (опционально v1).
- **Навигация:** из редактора всегда есть кнопка возврата **«На главную»**. Возможно, реализовано в виде табов нижнего меню (см. далее), тогда переход между Редактор ↔ Главное меню осуществляется по табу.
- **Экран Настройки:** содержит настройки пользователя и приложения, разделённые по категориям (см. также дизайн-систему best practices 2025). Ключевые группы настроек:
- **Доступность:** размер шрифта, цветовой контраст, отключение изображений/анимаций, автопроизведение TTS, отображение субтитров, специальный шрифт для дислексии.
- **Контент:** возрастные ограничения (фильтр ужасов/жести), пользовательские “линии и завесы” (список триггер-тем, которые не должны появляться), фильтр нецензурной лексики.
- **Геймплей:** скорость вывода текста (постепенно печатать или сразу), длина сцены (короткая/стандартная), частота подсказок AI, публичность бросков (видят ли другие результаты), детализация боя (например, показывать ли расчёты урона подробно).
- **Интерфейс:** макет кнопок действий (стандартный **5+1** или компактный **3+1**), размер интерактивных элементов (крупные кнопки), вибро-отклик при событиях.
- **Аудио/Видео:** громкость или выключение TTS/музыки, авто-приглушение фоновой музыки телефона при озвучке, качество генерируемых изображений (низкое для экономии или высокое).
- **Приватность:** настройка телеметрии (минимум vs стандарт), шаринг прогресса (можно ли делиться ссылкой на свою кампанию), скрывать ли свой Telegram-ник и аватар в пати.
- **Локализация:** выбор языка интерфейса (RU / EN), форматы времени/даты.
- **Уведомления:** управление push-уведомлениями (например, напоминание о своем ходу, приглашения в кампанию, получение лута).
- **Профиль GM (для мастеров):** тональность повествования по умолчанию (напр. героический или мрачный), строгость правил (RAW vs Cinematic), автогенерация артов (включить/отключить авто-арт при новых NPC/локациях), привязка аккаунта Telegram Stars для оплаты.  
    _Состояния:_ В норме настройки разбиты по секциям с переключателями, выпадающими списками и ползунками. При изменении настройки может показываться краткий toast “Сохранено”. Если какое-то изменение требует перезагрузки приложения – выдается предупреждение/CTA «Перезапустить для применения». В оффлайн-режиме изменение настроек ограничено, а неподтверждённые изменения сохраняются локально до связи с сервером.

### Карта навигации (сводная диаграмма)

Ниже представлена сводная схема экранов и путей пользователя:

\[Главное меню\]  
├─► \*\*Соло\*\* → \[Создание персонажа\] → \[Сцена (игра)\] ↦ (возможен переход в \[Бой\]) ↦ … (цикл сцен/боёв) → \[Финал/Эпилог\] → Главное меню  
├─► \*\*Играть в пати\*\*:  
| ├─ (Создать новую) → \[Лобби пати (хост)\] → (игроки подключаются) → \[Сцена игры (групповая)\] ↦ \[Бой\] ↦ … → \[Финал\]  
| └─ (Войти по коду) → \[Лобби пати\] → … (далее как выше, при старте)  
├─► \*\*Я — Мастер\*\* → \[Лобби пати (Co-DM)\] → (игроки подключаются) → \[Сцена (черновик/публикация)\] ↦ \[Бой\] ↦ … → \[Финал\]  
├─► \*\*Редактор\*\* → \[Каталог кампаний\] → \[Экран редактирования/конструктор\] (кампании, NPC и т.д.) ↦ (просмотр/тест) → … → Главное меню  
└─► \*\*Настройки\*\* → \[Экран настроек\] ↦ (подэкраны опций) → … → Главное меню

Легенда: стрелки → обозначают переход на другой экран, ↦ – смену режима в рамках игрового процесса, многоточие означает повторяющиеся шаги или продолжение сценария.

В этой карте отражены также альтернативные пути: например, **Продолжить** ведёт сразу в **Сцену (игра)** минуя создание персонажа и главное меню. После завершения кампании или при вручную выходе игрок возвращается на главное меню (при этом может появиться опция повторного прохождения или выбора другого режима).

## 2.3 Главное меню (компоновка)

**Выбранный вариант дизайна:** Для главной навигации приложения решено использовать **нижнюю панель** (bottom navigation) – вариант **B** из предложенных. Этот подход обеспечивает быстрый доступ к основным разделам и привычен для мобильных приложений. В нижней панели размещены 5 иконок-разделов: **Соло**, **Пати**, **Мастер**, **Редактор**, **Настройки**. Каждая иконка сопровождается короткой подписью и при нажатии переводит на соответствующий экран. Панель видна на всех основных экранах, кроме полноэкранных режимов игры (чтобы не отвлекать): во время активной сцены или боя панель может автоматически скрываться и заменяться игровым UI, однако доступна при паузе или через системное меню «...».

**Обоснование:** Нижняя навигация позволяет в один тап переключаться между ключевыми разделами. Альтернативный вариант с **карточной сеткой** (A) на главном экране тоже рассматривался, но от него решено отказаться в пользу постоянной панели, чтобы, например, можно было зайти в настройки или редактор контента прямо во время игрового процесса (панель появится при вызове меню). Вариант **ленты команд** (C) не выбран, так как он больше подходит для чат-бота без WebApp.

**Mobile-first реализация:** Интерфейс спроектирован под маленькие экраны смартфонов (ширина 360–414px). Нижняя панель адаптивна: на очень узких экранах иконки могут уменьшаться или скрываться подписи, на более широких – отображаются полностью. Все интерактивные элементы имеют размер касания **≥44px** в любом направлении (например, иконки bottom-nav имеют hit-area с внутренними отступами). Это гарантирует удобство на сенсорных экранах. Используются контрастные цвета для фона/текста (WCAG AA), читаемые шрифты не менее 14px. Предусмотрена поддержка **тёмной темы**: цвета фона и текста инвертируются при сигнале от Telegram (объект themeParams WebApp), иконки используют монохромные SVG, которые перекрашиваются в нужный цвет (или дублируются светлый/тёмный вариант).

**Композиция главного экрана:** При запуске, когда отображается главное меню, центральную часть экрана может занимать фирменный логотип или иллюстрация RPG-Bot и, возможно, приветственное сообщение. Ниже – набор крупных кнопок/иконок для каждого режима (если bottom-nav реализован как постоянный, то, возможно, дублирование не нужно; в противном случае на главном экране могут быть плитки **Соло / Пати / Мастер / Редактор** для наглядности). Например, реализовано как сетка 2×3 с пиктограммами: - 🎲 **Соло-приключение** – описание “Быстрая игра за 2 минуты”. - 🧑‍🤝‍🧑 **Играть в группе** – “Co-op режим на 2–5 игроков”. - 🧙 **Я — Мастер** – “Вести игру самому (Co-DM)”. - 📚 **Редактор приключений** – “Создать свой сюжет, NPC...”. - ⚙️ **Настройки** – “Глобальные настройки и профиль”.

_(Точный текст на кнопках может отличаться, но должен быть кратким.)_ При нажатии на каждую плитку происходит то же действие, что и выбор соответствующей вкладки нижней панели.

**Wireframe главного меню:**  
_Placeholder:_ Ниже будет представлен wireframe (макет) главного экрана. Он включает в себя: заголовок с названием игры, приветственный текст, иконки режимов (в виде сетки или горизонтального меню) и пример нижней навигационной панели. В макете также указаны отступы для адаптивности, и пример отображения в тёмной теме (инвертированные цвета). _(См. вложение Wireframes.pdf – экран «Главное меню».)_

## 2.4 Микрокопирайтинг и локализация

**Принципы микрокопирайтинга:** Тон сообщений интерфейса – дружелюбный, лаконичный, соответствующий жанру. Игра ведётся от второго лица, но системные уведомления стараются быть нейтральными. Например, при критической неудаче можно показать юмористичное сообщение в стиле игры, но ошибки системы (сетевые, технические) формулируются без излишней диалоги. Везде соблюдается **fail-forward** идея: даже сообщение о неудаче подаётся как часть опыта, а не тупик (напр., “Вы споткнулись, но продолжаете путь…” вместо “Ошибка: действие не выполнено”).

**Подтверждения и критичные действия:** Все действия, которые тратят ценный ресурс или необратимо влияют на игру, сопровождаются подтверждением. Например, при нажатии **«Сгенерировать арт»** всплывает диалог: _“Будет списано 1 ⭐ (звезда). Продолжить?”_ с кнопками **Да/Отмена**. Аналогично подтверждаются покупки Stars, сброс персонажа, выход из незавершённой кампании, удаление контента в редакторе. Текст подтверждения чётко указывает действие и последствие (списание, удаление и т.д.) без двусмысленности. включить авторежим где списываться будет автоматически но появляется счетчик списанных "ресурсов".

**Подсказки и инструкции:** В интерфейсе используются короткие подсказки (hints) там, где функциональность может быть неочевидна. Примеры: - При первом входе в лобби пати у хоста под кодом может быть подсказка “Поделитесь этим кодом с друзьями”. - Кнопка «Свой ход» может иметь tooltip “Напишите любое действие от себя”. - В редакторе под полем “Секреты NPC” пояснение “Лёгкий секрет раскроется быстро, трудный – позже”. - Если игрок бездействует, можно вывести ненавязчивый hint: “Можно в любой момент открыть инвентарь или спросить совета \[ASK\]”. Подсказки пишутся простым языком, в позитивном ключе, избегая фраз “вы должны” – лучше “вы можете”.

**Тосты (уведомления):** Для фоновых событий используются toast-уведомления – небольшие всплывающие сообщения, исчезающие через ~2-3 сек. Примеры: - “Сохранено” – при успешном сохранении настройки или контента. - “Подключение потеряно... попытка reconnect” – при проблемах с сетью. - “Новое достижение!” – при получении бейджа/титула. - “Ошибка генерации, попробуйте еще раз” – если провайдер LLM/TTS вернул ошибку.  
Тексты тостов максимально короткие, часто без точки, могут включать эмодзи для акцента (но в меру). Например: “📦 Предмет добавлен в инвентарь”.

**Терминология SRD:** Поскольку система основана на D&D 5e SRD, важно сохранять канонические термины. В русской локализации используются общепринятые переводы: - **Ability Scores:** STR/DEX/CON/INT/WIS/CHA обозначаются как Сила, Ловкость, Телосложение, Интеллект, Мудрость, Харизма (или кратко, напр. Сл, Лв, Тл, Ин, Мд, Хр — если мало места). - **Skill Checks:** использовать переводы навыков (Атлетика, Акробатика, Внимательность и т.д.) соответствующие фанатским версиям SRD. При этом в англ. версии — Athletics, Acrobatics и т.п. - **Advantage/Disadvantage:** переведено как “Преимущество/Помеха”. - **Saving Throw:** “Спасбросок”. - **Hit Points (HP):** можно оставить аббревиатуру “HP”, т.к. она универсальна, но в подсказке указать “Очки здоровья”. - **Long/Short Rest:** “долгий отдых/короткий отдых”. Все подобные термины собираются в глоссарий, чтобы переводчики соблюдали единообразие. Запрещено использовать закрытые термины или слэнг вне SRD — стиль должен быть понятен новичку, но и не коробить опытного игрока.

**RU/EN локализация и ключи i18n:** Весь текст интерфейса вынесен в ресурсы для удобства перевода. Формат – JSON с ключами. Ключи строятся по областям и смыслу, например:

{  
"menu.solo": { "ru": "Соло-приключение", "en": "Solo Adventure" },  
"menu.party": { "ru": "Играть в пати", "en": "Play in Party" },  
"menu.master": { "ru": "Я — Мастер", "en": "I am the DM" },  
"action.generate_art.confirm": {  
"ru": "Сгенерировать арт за {count}⭐?",  
"en": "Generate art for {count}⭐?"  
},  
"toast.saved": { "ru": "Сохранено", "en": "Saved" },  
"...": "..."  
}

Ключи строятся иерархически: menu.\* для главного меню, action.\* для подписей кнопок, toast.\* для уведомлений и т.д. Переменные (например, {count}) используются для чисел, имен и пр. При добавлении новых фраз сразу пишутся оба варианта (ru/en). База микротекстов хранится в артефакте **ux-copy.json** (см. ниже).

**Таблица тональности сообщений:** Для единообразия тона подготовлена таблица стилей для разных типов коммуникации:

| Категория сообщения | Тон и стиль (RU) | Пример формулировки |
| --- | --- | --- |
| **Нарратив/сцена** | Художественный, от второго лица, героический оттенок. | _“Ваша команда осторожно входит в полутёмный зал…”_ |
| **Системное (игровое)** | Нейтральный, лаконичный, можно в инфинитиве. | “Бросок: d20+2 = 15 (успех)” |
| **Ошибка/предупреждение** | Вежливый, без вины пользователя, с решением. | “Не удалось подключиться. Проверьте интернет.” |
| **Подсказка/хинт** | Дружеский, ободряющий, в форме совета. | “Совет: попробуйте осмотреться вокруг (действие).” |
| **Подтверждение** | Прямой, уточняющий, без лишних слов. | “Вы уверены, что хотите выйти из кампании?” |
| **Диалог мастера** | (если Co-DM общается) Краткий, от 1го лица мастера. | “Мастер: _Это решение приведёт к неожиданным последствиям…_” |

_(Примечание: фактическая таблица тонов оформлена в отдельном документе. Здесь приведены примеры.)_ Общий guideline: **максимум ясности**, **минимум канцелярита**. В английской версии аналогично – короткие фразы, никаких длинных объяснений на экране, только самое необходимое.

**Правила сокращений:** В русской версии стараемся не сокращать слова, кроме общепринятых аббревиатур (HP, XP, TTK и т.п.). Если место ограничено, допускаются сокращения с точкой или общепонятные: “Инв.” (Инвентарь), “Перс.” (Персонаж) в очень тесных кнопках. Английские термины типа **DM, NPC, TTS** сохраняются как есть (они международные). В UI-элементах, где не хватает места, применяем иконки вместо текста (например, значок 🗺️ вместо слова “Карта”). Все сокращения должны быть заранее утверждены, чтобы не было разночтений.

**Артефакты главы:** UX Flowchart, Wireframes.pdf, ux-copy.json

- **UX Flowchart:** диаграмма пользовательских потоков, показывающая основные ветки входа (согласие, продолжить, deep-link) и навигацию между экранами (см. раздел 2.2). В схеме отмечены состояния сессии (ключи, кеш) и роли (соло/пати/мастер). Формат – либо изображение, либо ASCII-схема, приведенная в тексте выше.
- **Wireframes.md:** набор из ~5–7 экранов с низкоуровневыми прототипами интерфейса. В вайрфреймы включены: Главное меню/навигация, экран Сцены (с примерами текста, кнопок действий, панелью AI-ярлыков), экран Боя (инициатива, действия), экран Лобби пати, редактор кампаний, настройки. Каждый wireframe показывает компоновку элементов, пример микрокопирайтинга (тексты кнопок, подсказки) и состояния: загрузка (spinner/«ждём…»), ошибка (сообщение), пустой список (-placeholder текст). Wireframes служат основой для UI-дизайна и будут уточняться дизайнером.
- **ux-copy.json:** файл локализации микротекстов, содержащий все ключевые строки интерфейса на двух языках. Структура – как описано выше (ключ -> объект с ru/en). В этом файле прописаны тексты кнопок, названия навыков, шаблоны сообщений (с переменными), тултипы, тосты, ошибки. Файл организован по разделам UI (menu, gameplay, editor, system, etc.) для удобства переводчиков и разработчиков. Этот артефакт нужен для проверки консистентности терминологии и для последующей настройки системы локализации.