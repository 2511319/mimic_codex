import type { FeatureManifest } from "../../feature/types";

import MediaFeature from "./view";

const manifest: FeatureManifest = {
  id: "media",
  title: "Media",
  Component: MediaFeature
};

export default manifest;
