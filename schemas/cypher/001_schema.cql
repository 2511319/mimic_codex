// Base schema scaffolding for project structure
// This file intentionally contains no data mutations by default.
// Use it to define indexes or seed relations patterns if needed.

// Example patterns (commented):
// MERGE (m:Module {project:$project, name:$module, path:$path});
// MERGE (c:Contract {project:$project, name:$contract});
// MERGE (d:DocSection {project:$project, doc_name:$doc_name, name:$section});
// MERGE (m)-[:IMPLEMENTS]->(c);
// MERGE (m)-[:DESCRIBED_IN]->(d);

